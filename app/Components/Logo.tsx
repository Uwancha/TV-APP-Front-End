import { Box, Typography } from '@mui/material'

export default function Logo() {
  return (
    <Box sx={{position: 'relative'}}>
      <Typography variant="h6" component={'p'} sx={{color:'white'}}>T-M</Typography>
    </Box>
  );
};